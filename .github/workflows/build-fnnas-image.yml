#=====================================================================================
# https://github.com/kuuer/fnnas
# Description: Build FnNAS Image for nanopi-r5s
#=====================================================================================

name: Build FnNAS Image (nanopi-r5s)

on:
  repository_dispatch:
  workflow_dispatch:
    inputs:
      fnnas_base_version:
        description: "Select fnnas base image version"
        required: false
        default: "rockchip_330"
        type: choice
        options:
          - rockchip_330
      fnnas_board:
        description: "Select fnnas device board"
        required: false
        default: "nanopi-r5s"
        type: choice
        options:
          - nanopi-r5s
      fnnas_storage:
        description: "Set fnnas image releases tags"
        required: false
        default: "fnnas_nanopi-r5s"
      fnnas_url:
        description: "Custom fnnas image download url"
        required: false
        default: ""
      builder_name:
        description: "Set fnnas builder signature"
        required: false
        default: "kuuer"
        type: choice
        options:
          - kuuer

env:
  TZ: Etc/UTC
  DTB_PATH: "${{ github.workspace }}/*.dtb"
  # 新增：固定内核下载地址
  KERNEL_URL: "https://github.com/kuuer/fnnas/releases/download/kernel_fnnas_rk3568_dsa/6.12.41-rockchip.tar.gz"
  KERNEL_FILENAME: "6.12.41-rockchip.tar.gz"

jobs:
  build:
    runs-on: ubuntu-24.04
    if: ${{ github.event.repository.owner.id }} == ${{ github.event.sender.id }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Initialization environment
        id: init
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          docker rmi -f $(docker images -q) 2>/dev/null || true
          [[ -n "${AGENT_TOOLSDIRECTORY}" ]] && sudo rm -rf "${AGENT_TOOLSDIRECTORY}"
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android 2>/dev/null
          sudo swapoff -a
          sudo rm -f /swapfile /mnt/swapfile
          sudo -E apt-get -y update
          sudo -E apt-get -y purge azure-cli ghc* zulu* llvm* firefox google* dotnet* powershell openjdk* mongodb* moby* || true
          sudo -E apt-get -y install $(curl -fsSL https://ophub.org/ubuntu2404-make-fnnas-depends)
          sudo -E systemctl daemon-reload
          sudo -E apt-get -y autoremove --purge
          sudo -E apt-get clean
          sudo sed -i '/NVM_DIR/d;/skel/d' /root/{.bashrc,.profile}
          sudo rm -rf ~/{.cargo,.dotnet,.rustup}
          sudo -E timedatectl set-timezone "${TZ:-Etc/UTC}"
          sudo -E ntpdate ntp.ubuntu.com 0.pool.ntp.org || true
          sudo -E timedatectl set-ntp true
          date -u
          timedatectl status || true
          echo "status=success" >> ${GITHUB_OUTPUT}

      - name: Create simulated physical disk
        run: |
          mnt_size=$(expr $(df -h /mnt | tail -1 | awk '{print $4}' | sed 's/[[:alpha:]]//g' | sed 's/\..*//') - 1)
          root_size=$(expr $(df -h / | tail -1 | awk '{print $4}' | sed 's/[[:alpha:]]//g' | sed 's/\..*//') - 4)
          sudo truncate -s "${mnt_size}"G /mnt/mnt.img
          sudo truncate -s "${root_size}"G /root.img
          sudo losetup /dev/loop6 /mnt/mnt.img
          sudo losetup /dev/loop7 /root.img
          sudo pvcreate /dev/loop6
          sudo pvcreate /dev/loop7
          sudo vgcreate github /dev/loop6 /dev/loop7
          sudo lvcreate -n runner -l 100%FREE github
          sudo mkfs.xfs -f -i sparse=0 -b size=4096 /dev/github/runner
          sudo mkdir -p /builder
          sudo mount /dev/github/runner /builder
          sudo chown -R runner:runner /builder
          df -Th
          echo "status=success" >> ${GITHUB_OUTPUT}

      - name: Download fnnas image file
        id: down
        working-directory: /builder
        if: (!cancelled())
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          [[ -d "fnnas" ]] || mkdir -p fnnas

          input_url="${{ inputs.fnnas_url }}"
          fnnas_base_version="${{ inputs.fnnas_base_version }}"

          if [[ -n "${input_url}" ]]; then
              echo "Custom URL detected: ${input_url}"
              clean_url="${input_url%%\?*}"
              regex="github\.com/([^/]+)/([^/]+)/releases/download/([^/]+)/(.+)"

              if [[ "${clean_url}" =~ $regex ]]; then
                  owner="${BASH_REMATCH[1]}"
                  repo="${BASH_REMATCH[2]}"
                  tag="${BASH_REMATCH[3]}"
                  filename="${BASH_REMATCH[4]}"

                  echo "Detected GitHub Release URL."
                  echo "Repo: $owner/$repo | Tag: $tag | File: $filename"

                  gh release download "${tag}" \
                      --repo "${owner}/${repo}" \
                      --pattern "${filename}" \
                      --dir "fnnas" \
                      --clobber

                  if [[ -f "fnnas/${filename}" ]]; then
                      echo "Download success via gh-cli."
                      echo "status=success" >> ${GITHUB_OUTPUT}
                      exit 0
                  else
                      echo "gh download failed."
                      exit 1
                  fi
              else
                  echo "Using curl fallback..."
                  latest_url="${input_url}"
                  fnnas_filename=$(basename "${clean_url}")
              fi
          else
              echo "Auto-detecting latest release..."
              latest_version=$(curl -s \
                  -H "Accept: application/vnd.github+json" \
                  -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
                  https://api.github.com/repos/kuuer/fnnas/releases?per_page=20 |
                  jq -r --arg RTK "fnnas_base_image" \
                  --arg BOARD "${fnnas_base_version}" \
                  '[.[] | select(.tag_name | contains($RTK))] |
                  map(.assets[] | select(.name | contains($BOARD) and endswith(".img.xz"))) |
                  sort_by(.updated_at) |
                  reverse |
                  .[0] |
                  {data: .updated_at, url: .url, name: .name}')

              [[ -z "${latest_version}" || "${latest_version}" == "null" ]] && {
                  echo "Invalid fnnas image file download address."
                  exit 1
              }

              latest_url="$(echo ${latest_version} | jq -r '.url')"
              fnnas_filename="$(echo ${latest_version} | jq -r '.name')"
          fi

          echo "Downloading: ${fnnas_filename}"
          echo "Source: ${latest_url}"

          curl -fsSL \
               -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
               -H "Accept: application/octet-stream" \
               "${latest_url}" \
               -o "fnnas/${fnnas_filename}"

          [[ "${?}" -ne "0" ]] && echo "Download failed." && exit 1

          fnnas_storage="${{ inputs.fnnas_storage }}"
          [[ -n "${fnnas_storage}" ]] || fnnas_storage="fnnas_save"
          echo "build_tag=${fnnas_storage}" >> ${GITHUB_ENV}
          ln -sf /builder/fnnas ${{ github.workspace }}/fnnas
          ln -sf /builder/fnnas /home/runner/work/_actions/kuuer/fnnas/main/fnnas

          echo "status=success" >> ${GITHUB_OUTPUT}

      - name: Download custom kernel (kernel_fnnas_rk3568_dsa)
        id: download_kernel
        working-directory: /builder/fnnas
        if: ${{ steps.down.outputs.status }} == 'success' && !cancelled()
        run: |
          # 创建内核目录
          mkdir -p kernel && cd kernel
          # 下载指定内核文件
          echo "Downloading custom kernel from: ${KERNEL_URL}"
          curl -fsSL "${KERNEL_URL}" -o "${KERNEL_FILENAME}"
          
          # 验证下载是否成功
          if [[ ! -f "${KERNEL_FILENAME}" ]]; then
              echo "Kernel download failed!"
              exit 1
          fi
          
          # 解压内核文件（按需，若构建脚本需要解压后的文件则执行）
          tar -zxf "${KERNEL_FILENAME}"
          echo "Kernel download and extract success."
          echo "kernel_path=/builder/fnnas/kernel" >> ${GITHUB_OUTPUT}
          echo "status=success" >> ${GITHUB_OUTPUT}

      - name: Copy root directory DTB files to build path
        if: ${{ steps.down.outputs.status }} == 'success' && !cancelled()
        run: |
          if ls ${DTB_PATH} 1> /dev/null 2>&1; then
              echo "Copying DTB files from root directory..."
              cp ${DTB_PATH} /builder/fnnas/
              ls -l /builder/fnnas/*.dtb
          else
              echo "No DTB files found in root directory!"
              exit 1
          fi

      - name: Packaging fnnas
        if: ${{ steps.down.outputs.status }} == 'success' && steps.download_kernel.outputs.status == 'success' && !cancelled()
        uses: kuuer/fnnas@main
        with:
          fnnas_path: fnnas/*.img.xz
          fnnas_board: ${{ inputs.fnnas_board }}
          # 强制指定内核路径，覆盖原有内核逻辑
          kernel_path: ${{ steps.download_kernel.outputs.kernel_path }}
          kernel_filename: ${{ env.KERNEL_FILENAME }}
          auto_kernel: false
          kernel_repo: kuuer/fnnas
          builder_name: ${{ inputs.builder_name }}
          dtb_path: ${DTB_PATH}

      - name: Upload the packaged fnnas
        uses: ncipollo/release-action@main
        if: ${{ env.PACKAGED_STATUS }} == 'success' && !cancelled()
        with:
          tag: ${{ env.build_tag }}
          artifacts: ${{ env.PACKAGED_OUTPUTPATH }}/*
          allowUpdates: true
          removeArtifacts: false
          replacesArtifacts: true
          makeLatest: true
          token: ${{ secrets.GITHUB_TOKEN }}
          body: |
            - Based on the official fnnas.com Arm64 release, for nanopi-r5s only.
            - Use custom kernel: 6.12.41-rockchip.tar.gz (from https://github.com/kuuer/fnnas/releases/download/kernel_fnnas_rk3568_dsa/)
            - Use DTB files from root directory.
            - 基于 fnnas.com 官方 Arm64 镜像构建，仅适配 nanopi-r5s 设备。
            - 使用自定义内核：6.12.41-rockchip.tar.gz（来自 https://github.com/kuuer/fnnas/releases/download/kernel_fnnas_rk3568_dsa/）。
            - 使用根目录下的 DTB 文件。
