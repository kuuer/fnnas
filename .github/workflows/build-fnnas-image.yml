#=====================================================================================
# https://github.com/kuuer/fnnas
# Description: Build FnNAS Image for nanopi-r5s
#=====================================================================================

name: Build FnNAS Image (nanopi-r5s)

on:
  repository_dispatch:
  workflow_dispatch:
    inputs:
      fnnas_base_version:
        description: "Select fnnas base image version"
        required: false
        default: "rockchip_330"
        type: choice
        options:
          - rockchip_330
      fnnas_board:
        description: "Select fnnas device board"
        required: false
        default: "nanopi-r5s"
        type: choice
        options:
          - nanopi-r5s
      fnnas_storage:
        description: "Set fnnas image releases tags"
        required: false
        default: "fnnas_nanopi-r5s"
      fnnas_url:
        description: "Custom fnnas image download url"
        required: true
        default: "https://github.com/ophub/fnnas/releases/download/fnnas_base_image/fnnas-official-arm64-image_rockchip_330.img.xz"
      builder_name:
        description: "Set fnnas builder signature"
        required: false
        default: "kuuer"
        type: choice
        options:
          - kuuer

env:
  TZ: Etc/UTC
  DTB_PATH: "${{ github.workspace }}/*.dtb"
  # 固定内核下载地址
  CUSTOM_KERNEL_URL: "https://github.com/kuuer/fnnas/releases/download/kernel_fnnas_rk3568_dsa/6.12.41-rockchip.tar.gz"
  CUSTOM_KERNEL_FILENAME: "6.12.41-rockchip.tar.gz"
  # 脚本内的内核路径（从错误日志提取）
  SCRIPT_KERNEL_DIR: "/home/runner/work/_actions/kuuer/fnnas/main/make-fnnas/kernel/6.12.41"
  SCRIPT_KERNEL_NAME: "6.12.41-rockchip.tar.gz"
  BACKUP_FNNAS_URL: "https://github.com/ophub/fnnas/releases/download/fnnas_base_image/fnnas-official-arm64-image_rockchip_330.img.xz"

jobs:
  build:
    runs-on: ubuntu-24.04
    if: ${{ github.event.repository.owner.id }} == ${{ github.event.sender.id }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Initialization environment
        id: init
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          docker rmi -f $(docker images -q) 2>/dev/null || true
          [[ -n "${AGENT_TOOLSDIRECTORY}" ]] && sudo rm -rf "${AGENT_TOOLSDIRECTORY}"
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android 2>/dev/null
          sudo swapoff -a
          sudo rm -f /swapfile /mnt/swapfile
          sudo -E apt-get -y update
          sudo -E apt-get -y purge azure-cli ghc* zulu* llvm* firefox google* dotnet* powershell openjdk* mongodb* moby* || true
          sudo -E apt-get -y install $(curl -fsSL https://ophub.org/ubuntu2404-make-fnnas-depends)
          sudo -E systemctl daemon-reload
          sudo -E apt-get -y autoremove --purge
          sudo -E apt-get clean
          sudo sed -i '/NVM_DIR/d;/skel/d' /root/{.bashrc,.profile}
          sudo rm -rf ~/{.cargo,.dotnet,.rustup}
          sudo -E timedatectl set-timezone "${TZ:-Etc/UTC}"
          sudo -E ntpdate ntp.ubuntu.com 0.pool.ntp.org || true
          sudo -E timedatectl set-ntp true
          date -u
          timedatectl status || true
          echo "status=success" >> ${GITHUB_OUTPUT}

      - name: Create simulated physical disk
        run: |
          mnt_size=$(expr $(df -h /mnt | tail -1 | awk '{print $4}' | sed 's/[[:alpha:]]//g' | sed 's/\..*//') - 1)
          root_size=$(expr $(df -h / | tail -1 | awk '{print $4}' | sed 's/[[:alpha:]]//g' | sed 's/\..*//') - 4)
          sudo truncate -s "${mnt_size}"G /mnt/mnt.img
          sudo truncate -s "${root_size}"G /root.img
          sudo losetup /dev/loop6 /mnt/mnt.img
          sudo losetup /dev/loop7 /root.img
          sudo pvcreate /dev/loop6
          sudo pvcreate /dev/loop7
          sudo vgcreate github /dev/loop6 /dev/loop7
          sudo lvcreate -n runner -l 100%FREE github
          sudo mkfs.xfs -f -i sparse=0 -b size=4096 /dev/github/runner
          sudo mkdir -p /builder
          sudo mount /dev/github/runner /builder
          sudo chown -R runner:runner /builder
          df -Th
          echo "status=success" >> ${GITHUB_OUTPUT}

      - name: Download fnnas image file
        id: down
        working-directory: /builder
        if: (!cancelled())
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          [[ -d "fnnas" ]] || mkdir -p fnnas

          input_url="${{ inputs.fnnas_url }}"
          fnnas_base_version="${{ inputs.fnnas_base_version }}"

          if [[ -z "${input_url}" ]]; then
              echo "未填写自定义镜像URL，使用备用地址：${BACKUP_FNNAS_URL}"
              input_url="${BACKUP_FNNAS_URL}"
          fi

          echo "开始下载基础镜像：${input_url}"
          clean_url="${input_url%%\?*}"
          fnnas_filename=$(basename "${clean_url}")

          curl -fsSL --connect-timeout 30 --max-time 600 --retry 3 \
               -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
               -H "Accept: application/octet-stream" \
               "${input_url}" \
               -o "fnnas/${fnnas_filename}"

          if [[ ! -f "fnnas/${fnnas_filename}" ]]; then
              echo "基础镜像下载失败！尝试备用地址：${BACKUP_FNNAS_URL}"
              curl -fsSL --connect-timeout 30 --max-time 600 --retry 3 \
                   -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
                   -H "Accept: application/octet-stream" \
                   "${BACKUP_FNNAS_URL}" \
                   -o "fnnas/${fnnas_filename}"
              
              if [[ ! -f "fnnas/${fnnas_filename}" ]]; then
                  echo "备用地址下载也失败！请检查URL是否有效。"
                  exit 1
              fi
          fi

          echo "基础镜像下载成功：fnnas/${fnnas_filename}"
          # 解压基础镜像（脚本需要.img文件，而非.img.xz）
          if [[ "${fnnas_filename}" == *.xz ]]; then
              echo "解压基础镜像..."
              xz -d "fnnas/${fnnas_filename}"
              fnnas_filename="${fnnas_filename%.xz}"
              echo "解压后文件名：${fnnas_filename}"
          fi
          fnnas_storage="${{ inputs.fnnas_storage }}"
          [[ -n "${fnnas_storage}" ]] || fnnas_storage="fnnas_save"
          echo "build_tag=${fnnas_storage}" >> ${GITHUB_ENV}
          ln -sf /builder/fnnas ${{ github.workspace }}/fnnas
          ln -sf /builder/fnnas /home/runner/work/_actions/kuuer/fnnas/main/fnnas

          echo "status=success" >> ${GITHUB_OUTPUT}

      - name: Pre-download custom kernel to script path (终极方案)
        id: pre_download_kernel
        if: ${{ steps.down.outputs.status }} == 'success' && !cancelled()
        run: |
          # 1. 创建脚本预期的内核目录（从错误日志提取的路径）
          sudo mkdir -p "${SCRIPT_KERNEL_DIR}"
          sudo chown -R runner:runner "${SCRIPT_KERNEL_DIR}"

          # 2. 下载自定义内核到脚本的内核目录，并重命名为脚本预期的文件名
          echo "下载自定义内核到脚本内核目录：${SCRIPT_KERNEL_DIR}/${SCRIPT_KERNEL_NAME}"
          curl -fsSL --connect-timeout 30 --max-time 300 --retry 3 \
               "${CUSTOM_KERNEL_URL}" \
               -o "${SCRIPT_KERNEL_DIR}/${SCRIPT_KERNEL_NAME}"

          # 3. 验证内核文件是否存在
          if [[ ! -f "${SCRIPT_KERNEL_DIR}/${SCRIPT_KERNEL_NAME}" ]]; then
              echo "内核下载失败！URL: ${CUSTOM_KERNEL_URL}"
              exit 1
          fi

          # 4. 提前解压内核（避免脚本解压失败）
          echo "提前解压内核文件..."
          tar -zxf "${SCRIPT_KERNEL_DIR}/${SCRIPT_KERNEL_NAME}" -C "${SCRIPT_KERNEL_DIR}"
          
          # 5. 验证解压结果
          ls -l "${SCRIPT_KERNEL_DIR}"
          echo "内核已提前下载并解压到脚本路径：${SCRIPT_KERNEL_DIR}"
          echo "status=success" >> ${GITHUB_OUTPUT}

      - name: Modify build script to use custom kernel URL (备用方案)
        if: ${{ steps.pre_download_kernel.outputs.status }} == 'success' && !cancelled()
        run: |
          # 找到脚本中拼接内核URL的文件（通用路径）
          SCRIPT_FILE="/home/runner/work/_actions/kuuer/fnnas/main/make-fnnas/fnnas.sh"
          
          # 检查脚本文件是否存在
          if [[ -f "${SCRIPT_FILE}" ]]; then
              echo "修改构建脚本中的内核下载URL..."
              # 替换脚本中的内核下载URL（匹配rockchip 6.12.41）
              sudo sed -i "s|https://github.com/kuuer/fnnas/releases/download/kernel_fnnas_rk3568_dsa/6.12.41-rockchip.tar.gz|${CUSTOM_KERNEL_URL}|g" "${SCRIPT_FILE}"
              # 替换所有6.12.41-rockchip.tar.gz的下载URL（兜底）
              sudo sed -i "s|6.12.41-rockchip.tar.gz|${CUSTOM_KERNEL_FILENAME}|g" "${SCRIPT_FILE}"
              echo "脚本修改完成，已替换内核URL为：${CUSTOM_KERNEL_URL}"
              # 查看修改后的脚本（验证）
              grep -n "6.12.41-rockchip.tar.gz\|kernel_fnnas" "${SCRIPT_FILE}" || echo "未找到匹配行，但已提前下载内核到路径"
          else
              echo "未找到构建脚本文件，跳过URL替换（已提前下载内核到路径）"
          fi

      - name: Copy root directory DTB files to build path
        if: ${{ steps.down.outputs.status }} == 'success' && !cancelled()
        run: |
          if ls ${DTB_PATH} 1> /dev/null 2>&1; then
              echo "Copying DTB files from root directory..."
              # 复制到基础镜像目录
              cp ${DTB_PATH} /builder/fnnas/
              # 复制到脚本内核目录（脚本可能从这里读取DTB）
              mkdir -p "${SCRIPT_KERNEL_DIR}/dtbs"
              cp ${DTB_PATH} "${SCRIPT_KERNEL_DIR}/dtbs/"
              
              # 安全的ls写法，避免报错
              echo "=== DTB files in /builder/fnnas/ ==="
              ls -l /builder/fnnas/ | grep .dtb || echo "No DTB files found (copy succeeded)"
              echo "=== DTB files in ${SCRIPT_KERNEL_DIR}/dtbs/ ==="
              ls -l "${SCRIPT_KERNEL_DIR}/dtbs/" | grep .dtb || echo "No DTB files found (copy succeeded)"
          else
              echo "警告：根目录未找到DTB文件！继续构建，但可能导致镜像无法启动。"
          fi

      - name: Packaging fnnas (use pre-downloaded kernel)
        if: ${{ steps.pre_download_kernel.outputs.status }} == 'success' && !cancelled()
        uses: kuuer/fnnas@main
        with:
          fnnas_path: fnnas/*.img
          fnnas_board: ${{ inputs.fnnas_board }}
          fnnas_kernel: "6.12.41"
          auto_kernel: false
          kernel_repo: kuuer/fnnas
          builder_name: ${{ inputs.builder_name }}
          dtb_path: /builder/fnnas/
        env:
          # 强制指定内核路径
          KERNEL_DIR: "${SCRIPT_KERNEL_DIR}"
          # 禁用curl重试（避免多次404）
          CURL_RETRY: "0"

      - name: Upload the packaged fnnas
        uses: ncipollo/release-action@main
        if: ${{ env.PACKAGED_STATUS }} == 'success' && !cancelled()
        with:
          tag: ${{ env.build_tag }}
          artifacts: ${{ env.PACKAGED_OUTPUTPATH }}/*
          allowUpdates: true
          removeArtifacts: false
          replacesArtifacts: true
          makeLatest: true
          token: ${{ secrets.GITHUB_TOKEN }}
          body: |
            - Based on the official fnnas.com Arm64 release, for nanopi-r5s only.
            - Use custom kernel: 6.12.41-rockchip.tar.gz (from https://github.com/kuuer/fnnas/releases/download/kernel_fnnas_rk3568_dsa/)
            - Use DTB files from root directory: rk3568-nanopi-r5s.dtb.
            - 基于 fnnas.com 官方 Arm64 镜像构建，仅适配 nanopi-r5s 设备。
            - 使用自定义内核：6.12.41-rockchip.tar.gz（来自 https://github.com/kuuer/fnnas/releases/download/kernel_fnnas_rk3568_dsa/）。
            - 使用根目录下的 DTB 文件：rk3568-nanopi-r5s.dtb。
